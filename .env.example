# Phaarvai - environment configuration
# Copy to .env.local for local development.
# Add the same keys in Vercel → Project → Settings → Environment Variables.

# Required for inbox delivery (defaults to partnerships@phaarvai.com if unset)
CONTACT_TO_EMAIL=partnerships@phaarvai.com

# FormSubmit works with only CONTACT_TO_EMAIL (no API key).
# After the first submission, open that inbox and click "Activate Form".
CONTACT_FORMSUBMIT=true

# Optional paid/provider email (takes priority when configured)
CONTACT_EMAIL_PROVIDER=auto
CONTACT_FROM_EMAIL=Phaarvai <noreply@phaarvai.com>
RESEND_API_KEY=
POSTMARK_SERVER_TOKEN=

CONTACT_PERSIST=true
CONTACT_DATA_DIR=.data
CONTACT_WEBHOOK_URL=
